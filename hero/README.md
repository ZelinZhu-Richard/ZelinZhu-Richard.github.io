# Hero Animation

Standalone extraction of the portfolio first-page particle hero animation.

## Run

From this folder:

```bash
npm install
npm run dev
```

If npm reports a peer dependency conflict on newer Node/npm versions, use:

```bash
npm install --legacy-peer-deps
```

## Test Production Build

```bash
npm run build
```
